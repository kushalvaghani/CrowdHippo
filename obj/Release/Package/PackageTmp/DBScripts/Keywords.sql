CREATE TABLE [dbo].[Keywords]
(
	[Id] INT NOT NULL PRIMARY KEY IDENTITY,
	[TweetId] INT NOT NULL FOREIGN KEY REFERENCES Tweet(Id),
	[SourceId] INT NOT NULL FOREIGN KEY REFERENCES Source(Id),
	[Keyword] NVARCHAR(MAX) NOT NULL
)
